#pragma once
// gfx906 port: stands in for the CUDA runtime header; this directory is
// prepended to the include path so the mechanical translation units keep
// their original #include <cuda_runtime.h>.
#include "core/hip_compat.h"
